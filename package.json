{
  "name": "pdf-generator",
  "version": "1.0.0",
  "description": "PDF generator with template upload support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/server.test.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "multer": "^2.0.2",
    "puppeteer-core": "^24.37.5"
  }
}
